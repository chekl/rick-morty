import React from 'react';
import { Link } from 'react-router-dom';

export default function Card() {
  return (
    <Link to={`/${}`}>
    <div>
      <img/>
      <h6></h6>
      <p></p>
    </div>
    </Link>
  );
}