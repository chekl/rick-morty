import React from 'react'

import Card from "../Card/Card"

export default function CharacterList() {
    if(false) {
        return(
            <h1>
              Not found
            </h1>
        )
    }
    return (
      <div>
        <Card />
      </div>
  )
}
